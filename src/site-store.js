// Filled in by main.jsx before the app is imported, so every module that
// reads data.js sees the saved products, settings and published customer
// feedback rather than the seed.
export const SITE = { products: null, settings: null, reviews: null };
