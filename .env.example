# Point the site at the Rust backend. Leave unset for preview mode.
# "/" sends requests to this site's own origin; in development Vite
# forwards /api to VITE_API_PROXY, so the admin cookie stays same-site.
# VITE_API_URL=/
# VITE_API_PROXY=http://localhost:8080
